#!/bin/bash

# arcalos:summary=List available monospace fonts
# arcalos:examples=arcalos font list | arcalos font set "CaskaydiaMono Nerd Font"

fc-list :spacing=100 -f "%{family[0]}\n" | grep -v -i -E 'emoji|signwriting|arcalos' | sort -u
