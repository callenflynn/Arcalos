#!/bin/bash

# arcalos:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/arcalos/toggles/hypr/internal-monitor-disable.lua"

if [[ -f $TOGGLE ]] && ! arcalos-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
