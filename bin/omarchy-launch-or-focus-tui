#!/bin/bash

# arcalos:summary=Launch a TUI or focus an existing terminal window for it
# arcalos:args=<command> [args...]

APP_ID="org.arcalos.$(basename "$1")"
LAUNCH_COMMAND="arcalos-launch-tui $@"

exec arcalos-launch-or-focus "$APP_ID" "$LAUNCH_COMMAND"
