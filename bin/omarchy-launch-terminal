#!/bin/bash

# arcalos:summary=Launch a terminal in the active terminal's current directory
# arcalos:args=[command...]

exec setsid uwsm-app -- xdg-terminal-exec --dir="$(arcalos-cmd-terminal-cwd)" "$@"
