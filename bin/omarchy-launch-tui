#!/bin/bash

# arcalos:summary=Launch a TUI command in the default terminal with arcalos styling
# arcalos:args=<command> [args...]

exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.arcalos.$(basename $1) -e "$1" "${@:2}"
