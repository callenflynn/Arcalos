#!/bin/bash

# arcalos:summary=Set the correct power profile on boot based on current AC/battery state.

if arcalos-battery-present && ! arcalos-ac-present; then
  arcalos-powerprofiles-set battery
else
  arcalos-powerprofiles-set ac
fi
