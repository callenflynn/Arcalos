#!/bin/bash

# arcalos:summary=Overwrite the user configs for swayosd (controls on-screen feedback for changing volume/songs etc) with the arcalos defaults and restart the service.

arcalos-refresh-config swayosd/config.toml
arcalos-refresh-config swayosd/style.css
arcalos-restart-swayosd
