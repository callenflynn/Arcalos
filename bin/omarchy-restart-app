#!/bin/bash

# arcalos:summary=Restart an application by killing it and relaunching via uwsm.
# arcalos:args=<application-name> [application-args...]

pkill -x $1
setsid uwsm-app -- "$@" >/dev/null 2>&1 &
