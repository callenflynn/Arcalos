#!/bin/bash

# arcalos:summary=Wrapper for swayosd-client that targets the currently focused monitor.
# arcalos:args=<swayosd-client-args...>

exec swayosd-client --monitor "$(arcalos-hyprland-monitor-focused)" "$@"
