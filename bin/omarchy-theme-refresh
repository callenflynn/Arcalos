#!/bin/bash

# arcalos:summary=Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.config/arcalos/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  ARCALOS_THEME_SKIP_BACKGROUND=1 arcalos-theme-set "$(cat $THEME_NAME_PATH)"
fi
