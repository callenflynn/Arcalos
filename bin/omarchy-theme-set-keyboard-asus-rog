#!/bin/bash

# arcalos:summary=Apply the current theme keyboard color to ASUS ROG keyboards
# arcalos:hidden=true

ASUSCTL_THEME=~/.config/arcalos/current/theme/keyboard.rgb

if arcalos-cmd-present asusctl; then
  asusctl aura effect static -c $(sed 's/^#//' $ASUSCTL_THEME)
fi
