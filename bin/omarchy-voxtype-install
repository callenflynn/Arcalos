#!/bin/bash

# arcalos:summary=Install and configure Voxtype dictation
# arcalos:requires-sudo=true

set -e

# Install voxtype and configure it for use.

if gum confirm "Install Voxtype + AI model (~150MB) to enable dictation?"; then
  arcalos-pkg-add wtype voxtype-bin

  # Setup voxtype
  mkdir -p ~/.config/voxtype
  cp $ARCALOS_PATH/default/voxtype/config.toml ~/.config/voxtype/

  voxtype setup --download --no-post-install
  if arcalos-hw-vulkan; then
    voxtype setup gpu --enable || true
  fi
  voxtype setup systemd

  arcalos-restart-waybar
  notify-send "    Voxtype Dictation Ready" "Hold F9 to dictate (or toggle with Super + Ctrl + X)." -t 10000
fi
