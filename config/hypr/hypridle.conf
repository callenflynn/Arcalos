general {
    lock_cmd = arcalos-system-lock                         # lock screen and 1password
    before_sleep_cmd = ARCALOS_LOCK_ONLY=true arcalos-system-lock    # lock before suspend without scheduling display off.
    after_sleep_cmd = sleep 1 && arcalos-system-wake                 # delay for PAM readiness, then turn on display.
    inhibit_sleep = 3                                      # wait until screen is locked
}

# Start screensaver after 2.5 minutes
listener {
    timeout = 150
    on-timeout = pidof hyprlock || arcalos-launch-screensaver
}

# Lock system after 5 minutes (screensaver resets idle timer, so have to just do half + 2s margin)
listener {
    timeout = 152
    on-timeout = arcalos-system-lock
    on-resume = arcalos-system-wake
}
