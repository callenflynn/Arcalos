# Toggles are used to turn certain features on/off in a persistent way
mkdir -p ~/.local/state/arcalos/toggles/hypr
cp "$ARCALOS_PATH/default/hypr/toggles/flags.lua" ~/.local/state/arcalos/toggles/hypr/
